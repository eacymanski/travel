<%= form_for(@destination) do |f| %>
  <% if @destination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@destination.errors.count, "error") %> prohibited this destination from being saved:</h2>

      <ul>
      <% @destination.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%if @destination.new_record? %>
    <%= f.label :city %><br>
    <%= f.text_field :city %>
    <%end%>
  </div>
  <div class="field">
     <%if @destination.new_record? %>
    <%= f.label :state %><br>
    <%= f.select :state, Destination::STATES %>
     <%end%>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
